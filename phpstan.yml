phpstan:
  stage: test
  variables:
    GIT_STRATEGY: fetch
  needs:
    - job: install_composer_dependencies
      artifacts: true
      optional: true
  dependencies:
    - install_composer_dependencies
  script:
    - 'phpstan analyse  --memory-limit=2G'
  allow_failure: true
