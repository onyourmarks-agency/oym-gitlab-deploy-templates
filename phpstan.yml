phpstan:
  stage: test
  variables:
    GIT_STRATEGY: fetch
  allow_failure: true
  needs: 
    - install_composer_dependencies
  dependencies:
    - install_composer_dependencies
  script:
    - 'phpstan analyse  --memory-limit=2G'
