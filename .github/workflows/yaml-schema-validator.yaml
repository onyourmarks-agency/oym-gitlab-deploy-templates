name: Validate-YAML

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  validate-yaml:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Validate YAML file
        run: "for file in $(find -E . -regex '.*\.(yaml|yml)'); do; yamllint $file; done;"
